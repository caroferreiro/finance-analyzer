# Reference Pack

Archival support folder from the earlier `Integrated + Unit` iteration model.

## Current Scope
- No active HTML pages remain under `reference_pack/`.
- The supported product surface is the root app at `/` (`web/index.html`).
- `assets/` and this folder's docs remain only as historical implementation context until a later cleanup pass removes them.

## Manifest
- `catalog.json` is intentionally empty because prototype/example pages were removed under `WTT-075`.
